spring:
  application:
    name: ms-cartoes
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 0
eureka:
  client:
    fetch-registry: false
    service-url:
      defaultZone: http://ms-eureka-user:WL&9yum3aQp(@localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}



mq:
  queues:
    cards-issuance: cards-issuance